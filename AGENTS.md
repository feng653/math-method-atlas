# 仓库工作约定

- 工作只在本仓库；禁止改动邻接 `control-visualization` 项目。
- 开始前阅读 `docs/MVP.md`、`docs/ARCHITECTURE.md`、`docs/IMPLEMENTATION.md`、`docs/ROADMAP.md`（如已存在），检查 Git 状态。
- 实现前用 `rg` 检索已有职责入口；扩展唯一实现，不创建重复 loader、schema、search、graph 或统计模块。新能力/路径迁移在同一提交更新速查表。
- **每个手写代码文件最多 250 行**，包括 TS/TSX/JS/CSS/测试/脚本；不要用压成一行规避。依赖、lockfile、构建产物不纳入；JSON 内容按体系/方法/题目分文件。接近上限即按职责拆分。
- 所有可计算信息来自纯函数；UI 不复制频次/覆盖逻辑。Zod schema 和 CLI 为内容校验入口。格式合法不代表数学正确。
- AI 编辑通过文本/JSON/CLI 完成，无须 computer use。新增体系按同一契约添加目录；ID 保持稳定，修订标题不能改引用 ID。
- 内容编辑流程：查找既有条目→修改最小文件→校验结构/引用→核对数学条件与来源→运行相关测试/构建→更新完成状态→提交并 push。校验失败必须修复，不能降低门禁。
- 真题记录区分目录、题目收录、来源核验、方法关联核验。只有核验关联参与频次；“尚未收录”不等于“未考查”。不得伪造题号、来源、频率或课纲完整性。
- 引用方法必须说明适用条件、步骤、易错点；例题注明自编。转载许可不明时仅存来源链接与原创摘要，禁止搬运整套受保护解析。
- 每个里程碑运行内容校验、代码行数检查、相关测试和生产构建，Git 提交保持职责清楚，再 push。未验证浏览器/部署必须直说；不得将计划写成已完成。
- 公开仓库禁止提交凭据、私人文件、node_modules、dist 和本地测试缓存。任何用户已有未提交改动都保留。
