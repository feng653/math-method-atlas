# 验证记录

## V2题型与公式首批（2026-09-08）

- 数学一22章新增73题型、99条条件公式，187张数学一方法均有题型入口；加证明工具体系共190方法。五张积分新卡、两张扩写方法独立二审通过；73题型仍draft，未宣称候选方法或历年归属穷尽。三份problem-types复核文档记录公式审查及修复。
- 52测试/9文件通过，47代码文件均≤250行；内容引用、公式解析、类型及生产构建通过。新增负例覆盖跨库题型/悬空引用、公式方法不在候选清单、重复ID、不由共享方法推题型；图测试覆盖共享节点和章节/题型聚焦的跨章方法。
- 生产预览43128默认283节点，其中73题型；搜索积分等式→8方法题型卡→分部积分卡可用。计算定积分卡显示2010Q10、2012Q10来源入口；未选入等式证明真题保持明确空状态。
- 题型type深链刷新恢复、关闭卡回章节、完整图谱返回、二级公式展开实测。章节图中的跨章克拉默节点已补，invalid type提示选有效题型时清除。
- 手机390×844的题型卡位于x10..380、y140..834，页面scrollWidth=390；恢复默认视口，生产预览错误日志为空。早期开发中的临时JSON未写完曾触发Vite错误覆盖层，最终生产版本无该错误；未将热更新中间态当发布证据。

## 弹性全图更新（2026-09-08）

- 默认完整数学一图谱实际渲染205节点（182方法、22章节、1中心），点击章节后15节点对应14方法，点击方法出现卡片；完整图谱按钮返回205节点并关闭卡片。
- 浏览器拖动中心约125px后，中心图坐标从(-110,-36)移动到(503,291)，相邻极限章从(-105,-650)移动到(-81,-636)，证明连带位移；拖动未误触导航。暂停、恢复布局与重新开启实测。
- 390×844手机宽度仍渲染205节点，页面scrollWidth=390，范围切换控件完整位于屏内；恢复默认视口。运行错误日志为空。真实触屏及系统减少动态切换仍未实测。
- 48测试通过，包括钉住拖动、二阶邻居传递、极端位移阻尼收敛/重置；43代码文件均≤250行。内容校验、类型检查及生产构建通过。此轮不改变内容核验边界。

2026-09-08。本地 `http://127.0.0.1:43127/`，Codex Browser 实际交互。

| 检查 | 证据 |
| --- | --- |
| 默认画布 | 1280×720截图：画布全屏、默认章节、无常驻内容栏 |
| 搜索→阅读 | 输入“分部积分”→唯一结果→对应节点与方法详情、公式、步骤呈现 |
| 局部浏览 | 关闭详情→适配图→一元积分11个方法可见 |
| 节点拖动/恢复 | 分部积分节点 transform 从(-470,224)变为(-398.366,174.752)，恢复按钮回到(-470,224) |
| 手机 | 390×844截图：搜索/目录/控制可操作，图适配35%；允许继续缩放阅读 |
| 手机多体系 | 目录切到proof-toolkit，只显示3方法；搜索反证+Enter打开对应方法 |
| 构建与内容 | 25测试（含碰撞/去重/引用/独立要求）、250行门禁、公式校验；命令每里程碑重跑 |
| 远端 | 首次GitHub Actions检查和Pages部署成功，run 34150173063 |
| 线上静态资源 | 第二次部署run 34154265507通过；HTML及其8个JS/CSS资源GET全部200、类型正确；线上浏览器导航超时，未把资源连通说成在线交互实测 |

尚未证实：所有移动设备的触屏手势、真实操作系统 reduced-motion 动态切换、全部外链长期可用性。reduce-motion 已在CSS与定位duration遵守；没有把静态检查说成这些设备实测。

完整方法图布局以210×60矩形作碰撞门禁，个别长标题换行的实际尺寸仍需浏览器持续抽检。外部课纲与真题源码可靠性见SYLLABUS_AUDIT与sources，数学编审见reviews。

本轮补验：Enter在章节节点进入局部图；Space在洛必达方法节点打开按需加载详情；跨体系非法method hash显示提示并返回总览；真题浮层加载18个年份。检查后浏览器error/warn日志为空。

后续恢复线上验证：Pages实际页面搜索“分部积分”显示唯一结果，已验证线上搜索交互；当前提交a7cb6b9的CI/Pages run 34155378676成功。其他交互仍以上述本地证据为准。

18个卷来源连通检查：17个HTTP 200，2022旧下载地址TLS错误，已更换为经全文核对且HTTP 200的HTML。此为当前连通证据，非来源官方认证或未来可用性保证。线上分部积分详情经Enter打开并确认标题显示。

画布空白区拖动(110,280)→(170,320)，viewport由translate(640,358.206)变为(700,398.206)，缩放不变；平移实际通过。门禁已扩至26测试，新增历史编号遗漏/重复/错误状态/悬空候选引用负例。内容提交fc5a032的Pages run 34155661810成功。

真题面板新增折叠的学科/方法筛选，本地浏览器选择2025年+线性代数，实际仅呈现第5/6/7/15/21题。下拉框显式可访问标签。长公式容器左端887.7px位于面板879.7px内，完整内容scrollWidth823px，可横向读取，不再居中截掉左端。

品牌入口实测关闭真题面板并恢复总览。497d3aa内容提交的CI/Pages run 34157200593成功。

## 完成度审计后的工程回归

- 图内部ID加类型前缀，内容ID/hash不变。搜索分部积分→真实节点 `method:hm-integration-by-parts` 选中，恢复布局后selected保留；关闭详情后对节点Enter仍打开正确方法hash；两条相关SVG连线有实际路径。
- 数学一范围显示2009—2026、18/18卷索引齐全。proof-toolkit无examScope，不显示数学一分母，空真题面板只有尚未收录提示。
- 临时增加合法空体系JSON（1章、0方法）实际浏览器切换：空搜索、目录0/1、空真题提示均通过；不改前端即可注册。测试后删除该临时体系，正式数据仍只有2体系。
- 共享archive-statistics测试覆盖：待审不计核验题数、重复记录/标签去重、同卷跨体系隔离、题号非连续不能称完整、目标缺失年份保持分母、范围外年份/其他考试不能填目标缺卷、无目标与空档案区分。
- 搜索空态提示改为通用关键词提示；无题库时隐藏无意义筛选控件，保持空面板简洁。

## 主辅角色验收

schema为已分类角色要求独立roleNote，缺字段展示待分类；角色不替代verification。单元验证待核主方法不计频次、已核验辅助方法计频次。所有408题已逐条角色编审，并补11条直接入口（涉及10道原仅辅助题）；当前457主/74辅/2待分，531条已核验关联。2026第22题两条未决关联仍pending与unclassified，未伪装完成。

浏览器筛选2012年+条件期望方法，实际第7题展示独立性“辅助方法”和塔式公式“主方法”；点击主方法正常打开同一内容ID的详情/hash。图谱首页没有增加常驻元素。具体逐题角色及数学推导见reviews/method-roles-*和primary-*。

## 来源元数据与子问结构

本批内容：18卷来源元数据、67题独立备用来源元数据，其他题按同一资源继承；95题HTML页图使用image-index。2021Q14来源网页可读但直接下载403，保留unverified并展示渠道差异。访问日采用已有2026-09-08实际记录，此次迁移不表示重新检查所有外部链接。三题7个真实子问由独立agent重新目视题面编号，其他题尚未逐一结构化。

43项测试通过，40个手写代码文件均≤250行，内容校验及生产构建通过。新增测试覆盖来源不同query不得继承、同文档hash可继承、独立题证据优先、非法日期/空依据拒绝、图片序号不生成PDF锚点、子问重复ID/原标号及悬空引用拒绝、两个子问同方法仍只计一题、父关联pending仍计零。

本地浏览器实测：2016年第23题展开两子问，三个原题链接均指向PDF物理第16页；2015年第21题展开两子问，均显示第4张试卷图，链接直接到文章且不再带伪#page。展开2015卷来源说明显示第三方、新东方发布者、检查日期及已知排印缺陷。来源和子问默认关闭，图谱仍挂载；本轮浏览器error/warn日志为空。系统动态reduced-motion及真实触屏设备仍未新增取证。

新发现启航转载2026卷也未明确第22题独立寿命条件；两条关联继续pending，详见2026-q22-source-followup.md。主问题总数、核验频次与主辅角色均未因子问增加而变动。全卷子问审计及2026官方原纲仍属后续工作。

## 逐卷子问边界审计

subquestionAudit区分已确认无编号和未审计；expectedCount与实际数组长度必须相符。archive-statistics与CLI共用已审题数、子问数、未审题ID、未映射及仅待核方法子问清单，重复主问题不重复累加子问。46项测试及生产构建通过，40代码文件均在250行内。

首批2009—2014全部138题已核编号边界：59个显式子问，另110题无显式编号。新增12条核验主关联补齐12子问入口；根agent独立核对其计算与方法条件。没有将“同题多个求值动作但无编号”强行拆分。后续卷逐批审计，精确数量运行content:report。

本地浏览器打开2014年第23题的“查看3个子问”，原题与三子问均定位KMF第13页；点击子问大数定律按钮得到pr-limit-lln方法hash与同名详情，图谱保持挂载。该行为实测不代替未完成卷的内容审校。

后续两组完成2015—2026，当前18卷408题全有编号边界审计，77题含164个末级子问，其余331题确认无显式编号。补30条父级核验关联，另2024Q18一元包络路线从辅改主；2017Q22第一子问独立备用来源身份已登记。浏览器2026Q22保留完整嵌套标号，前两子问显示待核；2017Q22第一子问指社区来源、第二子问指原新东方，独立来源元数据未误继承，日志无error/warn。

完成度复审仍发现未编号并列作答任务不可由子问统计替代。2012Q18已补面积π/4的主入口与无穷端点截断辅助，原建模关联保留；其他此类任务继续定点检查。官方2026原纲和两条pending关联仍未解决，未标总目标完成。

## 窄屏子问阅读回归

390×844浏览器视口实测2017Q22展开子问及独立来源说明：面板范围x=10—380、y=140—834，内容在detail-scroll内部滚动，无页面横向溢出。截图确认发布者、检查日期、补充版本说明及方法按钮均正常换行。关闭真题面板并适配当前图后，面板数为0，画布边界精确为390×844；随后已恢复默认视口。

当前浏览器提供viewport模拟，但未提供reduced-motion媒体模拟，故没有声称系统偏好动态切换已实测；已有CSS媒体规则与定位duration仍按原实现保留。上述是窄屏浏览器验证，不是实际触屏设备测试。

## 无编号作答目标复查

三组独立agent核对所有331道expectedCount=0题的实际作答目标，分别110/106/115题；检查集合、复合目标与现有路线见reviews/unnumbered-*。新增8条父级verified/primary入口：2009Q16全项和、2012Q17收敛域、2019Q2极值、2020Q17收敛、2022Q17渐近线/Q20充分方向/Q22方差、2025Q1拐点。根agent逐条独立核算新增数学推导；保持题数、子问数、原来源与两pending不变。2024Q18旧共用note的“无界端”修正为实际闭域边界与角点。

完整check的46项测试通过；新增内容不改变前端结构。本轮检查是目标入口覆盖，不宣称所有替代解法穷尽或官方原卷认证。2026官方要求正文仍未取得，已向用户请求可访问正文链接；该输入未到达前不能把历史要求核验升级为目标版完整覆盖。


## 自由拖拽与连线整理（2026-09-08）

56项测试、内容校验、TypeScript及生产构建通过；49个手写代码文件均≤250行。新增回归覆盖拖动联动、松手900步不回原位、重叠排斥、整体偏移3000步约束、共享方法主干唯一及连线端口方位切换。构建仍有内容分块超过500KB提示，未作性能容量承诺。

本地生产构建实测：完整图保留283节点，默认282条主干边，一键全部显示369条；悬停分部积分显示286条，点击仍进入积分章节并加载方法卡。根节点由约(-113,-32)拖至(386,294)，随后采样约(378,314)，没有返回原坐标；相邻章节也有联动。浏览器error/warn日志为空。减少交叉依靠主干筛选和朝向端口，不声称无交叉；布局不跨刷新保存。
390×844视口下控制条位于x=39—351，无页面横向溢出；测试后恢复默认视口。未新增真实触屏或系统减少动态切换取证。


## 默认分层布局（2026-09-08）

58项测试、内容校验、类型检查、生产构建通过，51个手写代码文件均≤250行。新增测试确认章节/题型/方法分层、卡片间距、共享方法主干归属在移动后保持不变，当前数学一默认主干的节点间直线段交叉数为0（不代表实际贝塞尔曲线或全部关联无交叉）。节点碰撞沿自身射线向外错开，避免横向挤入邻接分支。

最终生产资源index-D0N9kVnI在本地浏览器验收：283节点完整显示，内外层次及连续分支可见；全部关联入口仍保留369条边。点击积分章节正常进入局部，控制台无error/warn。全图为结构概览，文字仍须缩放或进入章节阅读；未改变拖动保位行为。


## 手机轻量模式测试分支（2026-09-08）

分支dev/mobile-dot-graph，正式main保持原版。60项测试、内容校验、类型检查与构建通过；53个手写代码文件均≤250行。调度器回归覆盖休眠后60秒无新增模拟、反复唤醒只有一个帧队列、暂停立即取消及再次唤醒。仍未实测手机温度/电池与真机持续拖动帧率。

本地生产浏览器验证：完整图283个圆点节点、0卡片，566个连接端口（原2264，减少75%）；章节概览23卡片及23副标题、0圆点。休眠后两次所有节点style采样完全一致；拖拽重新出现is-flowing，松手1.8秒后取消，根节点保持约(305,205)。390×844视口下283个标题均nowrap，页面无横向溢出。小体系也使用相同圆点入口。浏览器验收发现rAF作为对象方法调用会Illegal invocation，已改箭头包装并完成重新加载及上述交互取证。

性能收益边界：消除静止时本应用的持续图谱模拟与呼吸动画；交互期仍为DOM/SVG及成对排斥计算，尚未做真实设备耗电A/B。正式Pages不随dev分支推送部署。

基础思路内容复核：g=tf、g′=f+tf′、g″=2f′+tf″；径向二阶偏导和为4(g′+tg″)。t>1推方程并用t=1初值，g=ln t，f=ln t/t在e处取最大1/e。3卡片明确触发条件、失败边界与通法退路；补充章节不参与课纲覆盖的回归已加入retrieval测试。题库记录保持不变。

触发条件层：基础思路3组kind=trigger，普通题型不变。新增服务端渲染回归验证触发详情标签/引导文案、隐藏历年真题区与普通题型保留该区；触发条件绑定真题的负例由CLI校验拒绝。结构与公式校验、类型检查和生产构建通过，本轮未新增浏览器取证。

【历史记录：其中第29题的中点计算与回验结论错误，已由下述纠错取代，不作为有效证据】第28/29题思路复核：28由Py=Qx得f″−f′=−e^x，f=(4−x)e^x、u=yf+C，两偏导及初值均回代成立。29点积得q=x+yy′，中点抛物线给q=y²；v=y²满足v′−2v=−2x，v=(7/2)e^(2x)+x+1/2，取含0且v>0区间的正平方根，几何与初值回验成立。新增2策略卡与2触发条件，扩展平方量导数公式；61项测试/校验/构建通过，题库记录未增加。本轮为内容更新，未新增浏览器取证。

例题手机阅读修复：390×844浏览器实测第28题展开5步，正文16px，例题宽331px；所有题面与步骤公式均无横向溢出，页面scrollWidth=390。截图确认上下标/导数数学排版、分步标题和独立公式块；随后恢复视口。第27—29题共用该组件，其他旧例题仅按句分段，尚未全部转LaTeX。新增回归覆盖新步骤数学渲染、旧文本兼容和危险公式拒绝。


2026-09-08思路图谱增量：52道不同主问题、2009—2026全部18年、22个常规章节各至少2题，4类13个触发条件均有样本支撑。三科提案经统一归并，抽样范围与逐章矩阵见reviews/thinking-map.md。结构合法不等于来源官方认证，未把样本增加到考试频次。

66项测试通过，新增抽样范围/引用负例/分类图完整性/样本渲染回归；已有全图节点避碰、主干零直线交叉和多体系隔离回归仍通过。390×844浏览器验证分类聚焦→触发卡→展开2009Q8样本→全概率分解方法卡，正文16px、页面scrollWidth=390；截图检查无横向溢出。浏览器error/warn为空。视口已恢复。桌面也核对四分类、13个触发节点存在；未声称在真实手机上测试耗电或所有样本的外链实时可用性。生产构建通过，内容包约851kB（gzip约216kB），仍有既存大块提示；此次不改变动画调度。

第29题纠错已复核：中点横坐标为(x+q)/2，正确结果y=√(3e^(2x)+2x+1)，回代几何条件与初值成立；上方旧错误记录明确标为失效。

手机按钮换行修复：绝对定位居中容器原本采用 auto 宽度，叠加 thinking.css 的 flex-wrap 后在窄屏被挤成两行。改为显式内容宽度且不换行，分类返回独立置于上方；按钮保持44px高。390×844及320×740浏览器核对两个按钮同一Y坐标、胶囊约192px宽、页面无横向溢出，章节概览选中状态切换正常。分类返回与范围按钮、抽样说明分离。

2026-09-09 力导向与目录：用户否定自动放大局部/小地图及分栏排布，最终采用统一力引擎。原同心环与零直线交叉断言随布局契约变更被替换为确定性、实际264×32圆点标题避碰、全部方法/语义边保留、长连接吸引更强、超过旧520距离仍互斥；内容/schema/引用门禁未降低。最终67项测试、内容校验、63个代码文件250行检查、生产构建通过，保留既有内容包大于500kB提示。

本地1536×1000浏览器验证章节展开、拖动题型、关联方法位移和松手后稳定：方向梯度节点约(-448,-70)移动至(-443,-76)，目录/详情正常。桌面目录点选前后scrollTop均323.333px；375×812关闭详情后目录位置保留、scrollWidth=375。浏览器error/warn为空。未实测真实触屏与本轮reduced-motion设置；全图缩放阅读和长标题省略仍受屏幕空间限制，不宣称所有标题同时完整可读。初始布置不再保证连线无交叉。

参考：Obsidian官方仓库明确核心不开源（https://github.com/obsidianmd/obsidian-releases/blob/master/README.md），未克隆不存在的源码；仅参考官方图谱文档的中心力/排斥力/连接力/连接距离语义（https://obsidian.md/help/plugins/graph），不是复刻其私有实现。
最终1536×1000全图实测：314个节点均在独立画布区域内，适配约40%，MiniMap不存在；完整图标题世界字号26px、行高1.1，实际字号仍受缩放影响。最终CSS更新后生产构建再次通过。

宽松初始态验证：恢复4433c88的径向种子，测试确认全图settled=true且maxSpeed<0.12，横向范围大于种子的50%而小于种子；所有章节及第二体系均达到连续30步稳定条件。原确定性测试涉及两次更长模拟，单项超时改为15秒，不改变内容/碰撞/引用/物理断言。1536×1000本地截图确认314节点、大范围径向结构、无小地图，适配约13%；不声称缩略标题可直接阅读。生产构建通过，既有内容大块警告保留。

最终pnpm check：69项测试、64代码文件250行门禁、内容和TypeScript校验全部通过；浏览器error/warn为空。

2026-09-09 紧凑邻域布局：pnpm check通过72测试/17文件，66代码文件均<=250行；内容校验、类型检查和生产构建通过（保留既有内容chunk大小警告）。全图与所有章节收敛通过；邻域枚举与暴力结果一致，1000个远离节点仅读取1000次坐标，不做全对距离计算；远处无连接节点不动，远处连接仍吸引。浏览器1536×1000适配全图为27%（前版记录13%），314节点，未见error；仍不能保证全图所有长标题同时完整可读。

2026-09-09 拖动受力修复：连线张力下限为0，取消压缩推力；松手和键盘移动不再采纳异常连线长度。运行回调以速度/拖动状态延长模拟，稳定后休眠，暂停/后台/减少动态保留。74测试、66文件行数门禁、内容及类型校验、生产构建通过；新增压短连线与超过10秒持续运动回归。本次未做浏览器拖动回归。
