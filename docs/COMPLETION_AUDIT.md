# 阶段完成度独立审计

2026-09-08。对照 MVP、ARCHITECTURE、ROADMAP、IMPLEMENTATION，读取实际源码、内容与 VERIFICATION，执行只读内容报告及图节点冲突复现。此为本轮验收快照，不修改 goal 或阶段状态；后续修复以对应验证记录为准。

已具备阶段版本：双体系只读图谱与 JSON/CLI 编辑、搜索/缩放/章节/详情/键盘/手机主要链路；185 方法已审校（数学一182、证明工具3）；18卷408主问题均有核验入口。内部145个数学一topic已映射、历史123条有候选，都不能替代2026官方逐项验收。

| 验收项 | 具体证据与结论 | 下一动作 |
| --- | --- | --- |
| 图谱ID隔离 | `graph.ts` 使用裸方法ID，同时根为root、章节为chapter-*。实际构造合法method.id=root：validateAtlas无错，3节点仅2个唯一ID。现数据不冲突，但新增体系可触发。 | 已获授权修复图节点内部命名空间，保留内容ID/hash；补root及chapter-*回归，再补浏览器定位与键盘。 |
| 空库定义与证据 | schema要求至少1体系、1章节；这是注册后0方法的空库，不是删除所有配置。buildGraph(library,[])可构造根与章节，但原tests未覆盖此情形。 | 补注册体系0方法的图谱测试，文档明确配置边界；浏览器检验目录、搜索无结果和空真题提示。 |
| 多体系 | loader按目录glob；App先按libraryId过滤；route/coverage校验与手机proof切换有证据。没有发现现有两体系数据污染。 | 与ID修复一起验证同名内容ID仍只打开当前体系方法；不新增体系专属逻辑。 |
| UI业务统计集中 | 审计时PaperLibrary直接filter统计核验题、逐卷题数，MethodDetail直接展示数组长度；getMethodStats独立去重。多个展示入口可能采用不同口径。 | 主agent正集中到唯一统计纯函数；验收新增共享汇总测试，不由本审计重复实现。 |
| P5覆盖范围展示 | 审计时详情只展示卷/题数量，真题总览无目标年份区间与完整卷N/18；ROADMAP要求明确范围与分母。 | 主agent正在补UI范围；同时验证proof无考试目标时不显示数学一18卷分母，缺失年份不会被误报完整。 |
| P5主/辅方法 | schema的methodLinks只有methodId/verification/note，UI也没有主/辅标识。ROADMAP P5明确要求，但未实现。 | 在同一契约添加主/辅角色并逐题审校标注；迁移前用“未分类”，不能按数组顺序猜主方法。 |
| P5子问口径 | schema只接收主问题编号，编辑指南要求子问归主问题，可避免当前408题重复计次；没有结构化子问条目。 | 若按路线图录入子问，增加主问题内子条目及主键去重测试；现版本明确只保存主问题级索引。 |
| 来源级别与可用性 | source仅url/page，来源说明靠sourceNote或docs/sources。complete只核题号完整；第三方整理与高校托管不等于官方原卷。VERIFICATION已有来源连通取证，但UI无失效状态。 | 将既有访问日/发布者级别/正文方式及许可说明整理为可展示来源元数据；出现已证实失效时记录状态并提示，不能由URL存在判真伪。 |
| 2026第22题 | content:report仍有2条pending，虽然该题另有verified入口，因此“408题有核验入口”不是“所有关联均核验”。 | 按sourceNote取得独立性条件的可靠题面；没有证据则继续pending，不以全题已有关联覆盖此缺口。 |
| 官方2026课纲 | syllabus为draft，历史requirements为historical-only；2019原文123条不是2026原本。SYLLABUS_AUDIT已记录正版书目与缺全文证据。 | 取得可核验正版2026数学一正文，逐编号比对历史索引及各子要求；此前P2/P3全纲与P6总完成均不得授予。 |
| 总体验收 | VERIFICATION记录本地主要交互、线上搜索/详情和多次CI部署；真实触屏与系统reduced-motion动态切换尚无实测。新增本轮统计/图ID修复未在旧记录中验证。 | 对新增行为做针对性浏览器回归及check/build/CI；设备未测项保留明确边界，不把既有截图当所有环境证据。 |

优先顺序：已复现图ID缺陷/空方法测试 → 共享统计与范围展示（主agent负责）→ 主辅方法契约及标注 → 来源元数据/未决关联 → 正版2026逐项核验。最后一项需要外部正文，但其他确定缺项不能据此提前停止或把总目标标完成。

本轮后续修复：图节点统一为 `root:<id>`、`chapter:<id>`、`method:<id>`，内容ID仍在data.methodId，hash不变；选中、关系边、定位与键盘转接均使用对应内部ID。新增root/chapter-*内容ID、注册体系零方法及跨体系过滤回归。这里“空库”明确指已有合法体系清单与至少1章节、方法数组为空，删除全部体系配置不属于该输入契约。图谱纯函数验证不替代浏览器空态/点击/定位补验。

后续工程验收：图ID隔离及空方法体系已完成浏览器回归；共享统计与目标范围已落盘，单元验证缺卷不缩分母、异考试/范围外年份不填缺卷，proof不套数学一目标。具体证据见VERIFICATION“完成度审计后的工程回归”。主辅角色、来源元数据与2026正文等未完成项仍保留。
